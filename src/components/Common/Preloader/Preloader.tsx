import preloader from './../../../preloader.gif'
import React from 'react'

const Preloader = () => {
    return(
        <div>
            <img src={preloader} />
        </div>
    )
}

export default Preloader